[phases.setup]
nixPkgs = ["chromium", "nss", "atk", "cups", "libdrm", "mesa", "libxkbcommon"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install chromium"
]

[start]
cmd = "python slipiq_orchestrator.py --schedule"
